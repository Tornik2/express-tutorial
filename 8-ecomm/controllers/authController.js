const User = require('../models/User')

const register = async (req, res) => {
    res.send('good to go so go')
}

const login = async (req, res) => {
    res.send('good to go so go')
}

const logout = async (req, res) => {
    res.send('good to go so go')
}

module.exports = {register, login, logout}